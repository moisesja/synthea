# Lessons

- Add dated entries here after user corrections with the mistake pattern and a prevention rule.
- 2026-04-06: When the user narrows review scope, keep the response strictly inside that boundary instead of including adjacent validation concerns. Prevention rule: restate the exact requested slice and exclude unrelated findings unless the user asks for them.
- 2026-04-06: For non-trivial repo work governed by `AGENTS.md`, do not rely on an internal task file alone. Prevention rule: write the `tasks/todo*.md` plan and explicitly present the implementation plan to the user for check-in before editing files.
